// Function express/anonymous

// Parameters
 const sum = function( number1, number2 ){
    let total = number1 + number2;
    return total;
 }

 let number1 = 20;
 let number2 = 30;

 // Arguments
 console.log(`A soma dos números é de ${sum(number1, number2)}`);